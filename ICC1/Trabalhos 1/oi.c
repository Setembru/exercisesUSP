#include <stdio.h>

int divs(int N, int *pi);

int main(){
    int e;

    scanf("%d", &e);

    int v[20];
    divs(e, v);

    return 0;
}

int divs(int N, int *pi){
    pi = m[20]
    for(i = N - 1; i > 0; i--)
        if(N % i == 0)
            

}